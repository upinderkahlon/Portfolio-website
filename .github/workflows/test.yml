name: Test yml code

on:
  workflow_dispatch:

jobs:
  code:
    runs-on: ubuntu-latest
    steps:
      - name: Code run
        run: echo "Code ran"
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Code build
        run: echo "Code build"
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Code test
        run: echo "Code tested"
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Code deploy
        run: echo "Code deployed"
